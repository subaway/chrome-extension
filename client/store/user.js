import axios from 'axios'

/**
 * ACTION TYPES
 */

/**
 * ACTION CREATORS
 */

/**
 * THUNK CREATORS
 */

/**
 * INITIAL STATE
 */
const initialState = {}
/**
 * REDUCER
 */
export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
